// create a simple countdown timer function

// variables 

let countdownTime = 0;

// start countdown
function startTimer(params) {
    
}
// stop countdown 
function stopTimer(params) {
    
}

// time remaining 
function timeLeft(params) {
    return countdownTime - timer.timeLeft
}

// reset the countdown 
function resetCountdown(params) {
    // when countdownTime === 0
    timer.reset()
}

// a countdown function
function countdown(real_time) {
    // countdown in hours, mins, secs, ms
    let real_time = [0,1,0,0]

    // a while loop?
    // while countdown > 0
    // 
}